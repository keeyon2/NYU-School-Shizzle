package edu.nyu.cs.pqs.ps5;

import java.awt.Point;

/**
 * @author Keeyon
 * Contains all the messages that the Model will
 * send to it's listeners 
 */
public interface Drawable {
  
  /**
   * Inidicates that you should draw at this location
   * @param p Point with location of where to draw
   */
  public void drawAtPoint(Point p);

  /**
   * This indicates that it is time to clear the canvas
   */
  public void clearAllPoints();
}
