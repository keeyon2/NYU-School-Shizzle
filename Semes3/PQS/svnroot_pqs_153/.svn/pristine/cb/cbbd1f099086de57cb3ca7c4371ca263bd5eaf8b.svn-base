package edu.nyu.cs.pqs.ps1;

import org.junit.Test;

public class AddressBookTestUnitTests {
  
  @Test
  /**
   * Test if Main Method has no exception thrown
   * Best we can do without changing the code.
   * 
   * If we were to change code, we would need to take out
   * all the of the system.out.println and have tests be 
   * individual tests like what we have created in the other
   * test classes  
   */
  public void TestMainHasNoException() {
    AddressBookTest.main(new String[] {"arg1", "arg2" });
  }
  

}
