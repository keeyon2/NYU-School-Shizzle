package edu.nyu.cs.pqs.ps5;

/**
 * @author Keeyon
 * Multi Window canvas app that allows you to draw on multiple canvases 
 * and have the results display in all other canvases 
 */
public class Main {

  /**
   * @param args
   * Grabs the model singleton and creates three GUIS
   */
  public static void main(String[] args) {
    MultiWindowCanvasModel model = MultiWindowCanvasModel.getInstance();
    GUI gui1 = new GUI(model);
    GUI gui2 = new GUI(model);
    GUI gui3 = new GUI(model);
  }

}
