x =[-1:0.1:1];
%HypeT = (exp(2*x) - exp(-2*x))/(exp(2*x) + exp(-2*x))
% tanh = tansig(2*x);
y = tanh(2*x);
P = legendre(5, y, 'norm');

%plot(x, P(6,:), x, y);
PFive = (1/8)*(63*HypeT^5 - 70*HypeT^3 + 15*HypeT)
plot(x,y, x, PFive)
%P = legendre(1, -1:0.1:1);


%plot(x, P,x,tanh(x))